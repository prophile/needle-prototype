experiments:
  - name: First Test
    description: >
      We are testing whether a green button converts better than a red one.
    start-date: 2016-09-07
    branches:
      - name: control
        fraction: 0.2
        parameters:
          button-colour: red
      - name: test
        fraction: 0.2
        parameters:
          button-colour: green
    site-area: home
    kpi: conversion
    minimum-change: 0.01
    secondary-kpis:
      - mov
